server:
  port: 8791
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: service-ribbon-hystrix-turbine-stream-rabbitmq
#management:
#  port: 8081
turbine:
  app-config: service-ribbon
  cluster-name-expression: new String("default")
  combine-host-port: true